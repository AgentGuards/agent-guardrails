name: Deploy — Server

on:
  push:
    branches: [main]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy not configured yet
        run: echo "Server deploy will be enabled when hosting is configured"

# TODO: uncomment and configure for your hosting platform
