# LLM API key — optional, enables AI judge + report generation.
ANTHROPIC_API_KEY=
CORS_ORIGIN=http://localhost:3000
DATABASE_URL=
DIRECT_URL=
GUARDRAILS_PROGRAM_ID=
HELIUS_AUTH_HEADER=
HELIUS_WEBHOOK_SECRET=
JWT_SECRET=
# Optional model overrides (uses sensible defaults if unset).
# LLM_JUDGE_MODEL=claude-haiku-4-5-20251001
# LLM_REPORT_MODEL=claude-sonnet-4-5-20250514
MONITOR_KEYPAIR=
# RPC polling interval in ms (default 30000). Set to 0 to disable.
# POLL_INTERVAL_MS=30000
PORT=8080
SOLANA_RPC_URL=http://localhost:8899
